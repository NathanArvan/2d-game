import { Routes } from '@angular/router';
import { BattleMapUiComponent } from './components/battle-map-ui/battle-map-ui.component';

export const routes: Routes = [
    {path: '', component: BattleMapUiComponent}
];
